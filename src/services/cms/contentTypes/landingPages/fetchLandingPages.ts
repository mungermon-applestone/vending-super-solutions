
/**
 * Fetch landing pages
 * This is a simplified version that returns an empty array.
 */

import { LandingPage } from '@/types/landingPage';

export const fetchLandingPages = async (): Promise<LandingPage[]> => {
  return [];
};

export default fetchLandingPages;
