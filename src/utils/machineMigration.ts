
/**
 * Machine migration utility
 * This is a simplified version since we no longer need to migrate data.
 */

export const migrateMachinesData = async () => {
  console.log('Machine migration is no longer needed with Contentful CMS');
  return [];
};
