
import React from 'react';
import Index from './Index';

// This component re-exports the Index component to maintain compatibility with routing
const Home = () => {
  return <Index />;
};

export default Home;
